info:
  name: 讀取目前計數
  type: http
  seq: 1
  tags:
    - counter

http:
  method: GET
  url: "{{baseUrl}}/api/counter"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Default Response
    request:
      url: "{{baseUrl}}/api/counter"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "count": 0,
            "updatedAt": ""
          }
